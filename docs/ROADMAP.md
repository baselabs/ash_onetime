# Roadmap

Status is maintained in-file per row: **done**, **parked** (post-1.0, tracked on the
build parent [#12](https://github.com/baselabs/ash_onetime/issues/12)), or **open**
(the work-list). Rows are grouped by priority: **H1x** security/correctness (P0),
**H2x** hardening/ops (P1), **H3x** enhancement/coverage (P2). `Depends` names row IDs;
cross-row joins are deliberate. Row text describes the gap as it was authored; the
status column records where it stands now.

## Security / correctness (P0 — do first)

| ID | Status | What | Acceptance | Depends | Why |
|---|---|---|---|---|---|
| H10 | **done** | **External-recovery adversarial-absence proof** — prove the re-execution invariant holds against an adapter that returns a well-formed lying `:absent`; `safe_recover` (`lib/ash_onetime/external_recovery.ex`) trusts it unconditionally. slug:h10-external-recovery-absence-proof | (1) a property/contract test that feeds a lying-`:absent` adapter and asserts no double-spend of the side effect under a committed claim id; (2) a normative callout in `documentation/external-effects.md` stating the adapter MUST prove absence. — Resolved: the lying-absent test (`test/ash_onetime/external_recovery_test.exs`) and the normative adapter contract (`documentation/external-effects.md`) are in place, and [ADR-0001](adr/0001-single-use-keyed-effects.md)'s trust-boundary amendment records the design decision. | — | The idempotency guarantee reduces to adapter honesty (ADR-0001). |
| H11 | **done** | **Committed-claim worker: distinguish timeout from disconnect** — the DPoP fence's 30s committed-claim timeout originally surfaced as `:unknown` in `store_uncertainty`, indistinguishable from a real disconnect. slug:h11-worker-timeout-telemetry | (1) a distinct `:worker_timeout` result_class threaded from the timeout path; (2) the operations.md triage note naming the distinction; (3) a test asserting the two paths emit different classes. — Resolved: `:worker_timeout` is a live class in the `store_uncertainty` allowlist (`telemetry.ex`), the pool-saturated runbook triages it against `:disconnected`, and the telemetry tests assert the classes. | — | The headline v0.2.0 fence's operational hot-spot. |

## Hardening / ops (P1)

| ID | Status | What | Acceptance | Depends | Why |
|---|---|---|---|---|---|
| H20 | **done** | **Oban worker backoff + discard alert** — the workers originally shipped `max_attempts: 3` with no `backoff/1`; a discarded `PartitionWorker` strands a month of retention. slug:h20-oban-worker-backoff | (1) an explicit bounded, jittered `backoff/1` on each worker; (2) a documented Oban-discard alert in `operations.md` keyed on the `:ash_onetime_*` queues. — Resolved: all three workers declare the bounded linear+jitter backoff, the three-queue discard alert is in the operations guide, and [ADR-0005](adr/0005-oban-worker-backoff-and-discard-alert.md) records the corrected rationale (spaced DDL-class retries). | — | The one worker where retry timing matters most is the retention-safety path. |
| H21 | **done** (acceptance revised by D3) | **Telemetry default handler** — the library emitted events but attached nothing; a fresh consumer saw nothing until they hand-rolled a handler. slug:h21-telemetry-default-handler | Original: counters surface in a standard dashboard without custom code. — Revised by D3: `AshOnetime.Telemetry.attach/0` is a pure router re-emitting all 13 events as `[:ash_onetime, event, :metric]` with normalized measurements; a consumer reaches a running dashboard with `attach()` at boot plus their existing `Telemetry.Metrics` reporter — no hand-rolled event enumeration or count/duration split, no `telemetry_metrics` dependency for the library. | H11 | AGENTS.md contract ("idempotency must surface unavailability via telemetry") is met by emitting; this closed the gap to operationally usable. |
| H22 | **done** (option b) | **Telemetry span (start/stop) structure** — every event is a point event with `:duration`; no `*.start`/`*.stop`/`*.exception`. slug:h22-telemetry-span-structure | Either span events, or an explicit "span-style out of scope" decision documented in `telemetry.md` with a recommended wrapping `:telemetry.span`. — Resolved via option (b): the point-events-only decision (and its leakage rationale — `span/3` force-injects context metadata and emits pre-rescue exception terms) is documented in `telemetry.md` and recorded in [ADR-0006](adr/0006-point-events-observability.md); consumers wrap their own call sites for trace correlation. | — | p99/SLO work does not require spans; value-free metadata does. |
| H23 | **done** | **operations.md runbook section** — the doc described failure-mode characteristics but no "if X then Y" procedures. slug:h23-operations-runbook | a Runbooks section with named procedures (backlog-stuck, partition-discard-detected, pool-saturated) and the exact SQL/telemetry query each uses; partition-discard detection includes a query/alert. — Resolved: the three named procedures exist and, since the operations-runbooks slice, every diagnostic snippet is executable (SQL row counts, a store-uncertainty tally, a checkout-queue watch) — each proven against the live test database. | H11, H20 | The ops doc for a security library; the gaps mapped to H11/H20 directly. |

## Enhancement / coverage (P2)

| ID | Status | What | Acceptance | Depends | Why |
|---|---|---|---|---|---|
| H30 | **done** | **Ship an ETS cache reference adapter** — originally only `cache/none.ex` shipped; the cache-degradation path was well-engineered but unrealized without a reference adapter. slug:h30-ets-cache-adapter | `AshOnetime.Cache.Ets` (bounded, TTL-aware) as an optional reference adapter under the existing `AshOnetime.Cache` behaviour. — Resolved: `lib/ash_onetime/cache/ets.ex` ships — deadline-based expiry, a max-entries cap with oldest-by-deadline eviction, supervision documented, and explicitly not admission authority. | — | The cache feature's value is unreachable without a consumer-usable adapter. |
| H31 | **parked** (post-1.0) | **Direct unit tests for `admission.ex`'s pure decision functions** — `admission_test.exs` exists but exercises the module transitively; the pure decision functions (`resolve/5` branches, `sanitize_request`/`sanitize_claim` stripping) have no direct coverage independent of a live Postgres. slug:h31-admission-unit-tests | a focused direct-coverage suite for the pure decision functions, independent of a live Postgres. — Parked on the build parent's [#12 post-1.0 parking lot](https://github.com/baselabs/ash_onetime/issues/12): the 599-test suite plus the ~113-entry mutation battery already pin the admission core; this is depth, not contract. | — | A regression in `emit_uncertainty` or `sanitize_request` should surface directly, not via integration. |
| H32 | **done** | **Direct tests for `key_source.ex` + `store/claim.ex`** — the key_source invariants (non-empty, ≤16 sources, no nesting, unique, valid tags) originally had no property tests. slug:h32-key-source-claim-tests | property tests for each `key_source` invariant boundary + structural tests for the `Claim`/`Claim.Request` structs. — Resolved: `key_source_test.exs` carries the iff-property over all five invariants plus a second property and boundary-exact rejection tests. | — | Input-validation rules on the security boundary are worth property-testing. |
| H33 | **done** (acceptance revised by D4) | **Document the runtime security-surface API** — the DSL was documented, but `token.ex`, `store.ex`, `key_source.ex`, `fingerprint.ex`, `telemetry.ex` carried `@spec` without `@doc`. slug:h33-runtime-api-docs | `@doc` on every spec-bearing public function in those modules. — Revised by D4: `token.ex`, `key_source.ex`, `fingerprint.ex`, and `telemetry.ex` are fully documented; `store.ex` is ruled **internal-by-design** (`@doc false` = internal per D1, the architecture census is the drift guard) — operator docs may name Store functions as mechanisms, but every executable path they hand operators is public (mix task, SQL, worker schedule, doctor output). | — | An undocumented *public* security surface is open risk; internal-by-design is not that. |
| H34 | **done** | **Dep-bound / CI-matrix documentation** — the bounds allow future drift; the CI matrix is the real guard, and that was undocumented. slug:h34-dep-bound-docs | CONTRIBUTING.md documents that compatibility is CI-matrix-asserted (not bound-asserted), or the bounds tighten. — Resolved: CONTRIBUTING.md's dependency-compatibility section names the matrix as the real guard with the matrix-extension rule for `ash_postgres`/`spark` majors (the per-cell vs release battery split is stated precisely in the same file). | — | Nothing else protects against a future AshPostgres/Spark major shifting semantics the fail-closed logic depends on. |

## Post-1.0 parking lot

Tracked on the build parent ([#12](https://github.com/baselabs/ash_onetime/issues/12);
the original map issue #1 is closed and hands off there): **H31** (direct admission
decision-function coverage) and the **test-lifecycle migration** (21 `Process.sleep`/
`Process.alive?` sites across 11 files to monitored/message-based synchronization,
including the named `RollContentionTest` sandbox-owner flake) are deliberate post-1.0
work, not 1.0 blockers.
